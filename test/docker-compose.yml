version: '3.6'
services:
    mongodb:
        image: mongo:latest
        environment:
            - MONGO_DATA_DIR=/data/mongodb
            - MONGO_LOG_DIR=/dev/null
            - MONGODB_DBNAME=opal
        command: mongod --smallfiles --logpath=/dev/null # --quiet
        ports:
            - 27017:27017

    algoservice:
        image: shubhamjain0594/opal-algoservice
        depends_on:
            - mongodb
        links:
            - mongodb
        ports:
            - 3001:3001
        expose:
            - 3001
        command: sh -c './src/wait-for.sh mongodb:27017 -- npm start'

    tests:
        build:
            context: ..
            dockerfile: ./test/Dockerfile
        tmpfs:
            - /tmp
        depends_on:
            - mongodb
            - algoservice
        links:
            - mongodb
            - algoservice
        privileged: true
        cap_add:
            - sys_admin
        ports:
            - 80:80
#        test command
#        command: sh -c 'sudo mount -tsecurityfs securityfs /sys/kernel/security && sudo apparmor_parser -r -W /etc/apparmor.d/usr.venv.sandbox.bin.python && mkdir /tmp/codejail-test/ && cp ./test/unlim.py /tmp/codejail-test/ && /usr/venv/sandbox/bin/python /tmp/codejail-test/unlim.py'
        command: sh -c 'sudo mount -tsecurityfs securityfs /sys/kernel/security && sudo apparmor_parser -r -W /etc/apparmor.d/usr.venv.sandbox.bin.python'
        security_opt:
            - apparmor:unconfined
